# DEMS Component Information

The `dems` component is designed for reading and passing mass fluxes (emissions).
It defaults to conservative regridding to ensure mass conservation.
Standard fields advertised by this component include `NOx`, `CO`, and `Dust_Flux`.
